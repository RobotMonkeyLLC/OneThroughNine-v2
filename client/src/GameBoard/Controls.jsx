const Controls = () => {
    return (
        <div className="controls-container">
            <button id="Return" className="control">Return</button>
            <button id="Restart" className="control">Restart</button>
            <button id="Undo" className="control">Undo</button>
        </div>
    )
    
}

export default Controls